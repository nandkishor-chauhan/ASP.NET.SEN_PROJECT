@using Microsoft.AspNetCore.Identity

@inject SignInManager<IdentityUser> SignInManager
@inject UserManager<IdentityUser> UserManager

<ul class="navbar-nav" dir="rtl">
    @if (SignInManager.IsSignedIn(User))
    {
        var currentUser = await UserManager.GetUserAsync(User);
        @* var userRole = await UserManager.GetRolesAsync(currentUser); *@
        @* string userRoleName = userRole.FirstOrDefault() ?? "No Role"; *@
        <li class="nav-item">
            <a class="nav-link text-dark" asp-area="IndividualEducationPlan" asp-controller="Student" asp-action="Create">
              الأهداف بعيدة المدى Long Term
            </a>
        </li>
        <li class="nav-item">
            <a class="nav-link text-dark" asp-area="IndividualEducationPlan" asp-controller="Student" asp-action="ShortTerm">
              Short Term  الأهداف قصيرة المدى
            </a>
        </li>
        <li class="nav-item">
            <a class="nav-link text-dark" asp-area="IndividualEducationPlan" asp-controller="Student" asp-action="IndividualPlan">
                IPlan إعداد الخطة الفردية
            </a>
        </li>
       
        <li class="nav-item">
            <a id="manage" class="nav-link text-dark" asp-area="Identity" asp-page="/Account/Manage/Index" title="Manage">
             Preview Plan معاينة الخطة
            </a>
        </li>
        <li class="nav-item">
            <a id="manage" class="nav-link text-dark" asp-area="Identity" asp-page="/Account/Manage/Index" title="Manage">
              Print Plan طباعة الخطط
            </a>
        </li>
    }
    else
    {
        <li class="nav-item">
            <a class="nav-link text-dark" id="register" asp-area="Identity" asp-page="/Account/Register">Register</a>
        </li>
        <li class="nav-item">
            <a class="nav-link text-dark" id="login" asp-area="Identity" asp-page="/Account/Login">Login</a>
        </li>
    }
</ul>
